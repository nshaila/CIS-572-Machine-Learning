to run:

type in your terminal:

python id3.py training_set test_set output_model

there is a fourth argument entropy which is default. to change the value of chi-squared test you have to give it and the desired value in the fifth argument

python id3.py training_set test_set output_model entropy 0.0
