to run:

type in your terminal:

$ python logistic.py training_set.csv test_set.csv model.txt eta sigma maxiter

Fourth & fifth arguments (eta, sigme) take floats. The output file name is passed as third argument. It has the bias and weight of the training data. 

